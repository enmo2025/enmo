commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}

pre-commit:
  commands:
    lint:
      run: pnpm lint
    
pre-push:
  commands:
    build:
      run: pnpm build